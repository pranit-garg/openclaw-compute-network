/**
 * Legacy App.tsx — not used when expo-router is the entry point.
 * The app entry is now "expo-router/entry" (set in package.json "main").
 * Screens live in app/(tabs)/ using file-based routing.
 */
export default function App() {
  return null;
}
