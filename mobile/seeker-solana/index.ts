/**
 * Legacy entry point — not used when expo-router is active.
 * The actual entry point is "expo-router/entry" (set in package.json "main").
 *
 * If you need to go back to the non-router setup, change
 * package.json "main" back to "index.ts" and uncomment below.
 */

// import { registerRootComponent } from 'expo';
// import App from './App';
// registerRootComponent(App);
